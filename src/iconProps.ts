/**
 * Props for configuring icon components.
 */
export default interface IconProps {
  /**
   * The size of the icon (in pixels).
   */
  size?: number

  /**
   * The color of the icon (as a CSS-compatible string).
   */
  color?: string
}
